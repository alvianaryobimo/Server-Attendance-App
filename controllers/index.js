const { adminControllers } = require("./adminControllers");
const { authControllers } = require("./authControllers");
const { attendanceControllers } = require("./attendanceControllers");

module.exports = {
    adminControllers,
    authControllers,
    attendanceControllers
};